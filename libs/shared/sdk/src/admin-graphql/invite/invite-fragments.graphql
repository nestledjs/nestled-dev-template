fragment AdminInviteList on Invite {
  id
  createdAt
  updatedAt
  expiresAt
  email
  token
  inviterId
  organizationId
  status
  roleId
  inviter {
    id
  }
  organization {
    id
  }
  role {
    id
  }
}

fragment AdminInviteDetails on Invite {
  ...AdminInviteList
}
