fragment AdminPlanList on Plan {
  id
  createdAt
  updatedAt
  name
  description
  price
  interval
  features
  limits
  active
  stripeProductId
  stripePriceId
  trialPeriodDays
}

fragment AdminPlanDetails on Plan {
  ...AdminPlanList
}
