fragment OrganizationList on Organization {
  id
  createdAt
  updatedAt
  name
  images {
    id
    url
    publicUrl
    filename
    mimeType
    metadata
    folder
    createdAt
  }
}

fragment OrganizationDetails on Organization {
  ...OrganizationList
  members {
    id
    userId
    roleId
    user {
      id
      firstName
      lastName
    }
    role {
      id
      name
    }
  }
  roles {
    id
    name
    description
  }
}
