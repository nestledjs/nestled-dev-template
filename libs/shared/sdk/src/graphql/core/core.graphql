query Uptime {
  uptime
}

fragment CorePagingDetails on CorePaging {
  count
  take
  page
  skip
  total
  filteredTotal
  pages
  hasNext
  hasPrev
}

# Import auth fragments
#import "../auth/auth-fragments.graphql"
