# Code Pipeline Run — 2026-08-06 12:11

## Summary

- Tasks found: 1
- Actions taken: 8
- Needs attention: 0 (one task in an active fix loop, no human decision required)

## Task Log

| Task | Stage | Action | Result |
|------|-------|--------|--------|
| PIR-260 | 3 | Branch recovery — FD was `DISPATCHED` with no branch; session had pushed `claude/per-project-dev-ports-rjzlcx` (`2a817b8`) | `STAGE_3_CONNECTED` |
| PIR-260 | 3 | Opened PR #66 via `gh pr create` (3 sessions in flight → canonical prefers `gh` over the shared-tab `create_pr`), verified `headRefName` | `STAGE_3_PR_OPENED_ONBOX` |
| PIR-260 | 3 | Reported branch + PR to FlightDesk; FD auto-advanced to `PR_OPEN` → `REVIEW_RUNNING` | `STAGE_3_PR_REPORTED` |
| PIR-260 | 4 | Ran all quality gates on-box (the cloud session's container had no working install, so it had run none) | 5 pass, 1 fail |
| PIR-260 | 4 | Fixed `format:check` failure on `docs/dev/dev-ports.md` with prettier, committed `b5be003`, injected pull notice | pushed |
| PIR-260 | 4 | Ran the plan's Docker-dependent manual checks (2, 6, 7 + port-move verification) | all pass |
| PIR-260 | 4 | Adversarial verifier (fresh-context `claude -p`) on `b5be003` | **`NEEDS_WORK`** |
| PIR-260 | 4 | Independently reproduced the verifier's blocking finding, then injected both items to the session; posted sign-off on Linear | stays `In Progress` |

## Gate results (on `b5be003`)

| Gate | Result |
|---|---|
| `pnpm nx test config` | pass — 31/31, 4 suites |
| `pnpm typecheck` | pass — 10 projects |
| `pnpm nx run-many -t lint` | pass — 0 errors, 3 pre-existing warnings |
| `pnpm nestled-doctor` | pass |
| `pnpm template:validate-upgrade-notes` | pass |
| `pnpm format:check` | **fail** → fixed on-box in `b5be003`, then pass |

Manual checks run on-box (Docker daemon available here, unlike in the cloud session): compose volume
sources still resolve under `.dev/tmp/` (the `--project-directory` guard holds); defaults resolve to
5432/5433/6379/1025/8025; a block-1 `.env` moves every host port; `test-db.sh url` returns 5443 when
`.env` sets `POSTGRES_TEST_PORT=5443`, matching what compose publishes; the doctor warns on each
half-moved pair and exits 0; `pnpm test:db:start`/`stop` both work (previously broken — finding 5).

The repo's local `.env` was backed up before the mutation tests and verified restored byte-identical
(md5 `596fe8253bcc50e81ab908f078b43da9`). All test containers and networks were removed afterward.

## Verifier verdict — `NEEDS_WORK`

Scope coverage was complete: every Definition-of-Done bullet in the plan is implemented, and every
claimed fix is actually present in the code rather than merely asserted. Two blocking items:

1. **CORS fallback follows `HOST` at runtime, contradicting plan decision D3 and shifting a default.**
   `webOrigin()` prefers `process.env['WEB_URL']`, but `validation.ts:18` defaults `WEB_URL` to a
   `HOST`-derived origin, and `@nestjs/config` writes validated defaults into `process.env`
   (`config.module.js:99`) *before* the load factory runs (line 105). I did not take this on trust —
   I reproduced it through the real validate → assign → `configuration()` ordering:

   | Config | `develop` today | This branch |
   |---|---|---|
   | `HOST=0.0.0.0`, no `ALLOWED_ORIGINS` | `[]` → `main.ts` fallback → `["http://localhost:4200"]` | `["http://0.0.0.0:4200"]` |

   A browser at `localhost:4200` is CORS-rejected on every request. New silent failure, and the
   `WEB_PORT` fallback branch is dead code in the running API. The feature's main case is unaffected
   and verified working (`HOST` unset + `WEB_PORT=4201` → `["http://localhost:4201"]`).

   The guarding spec (`it('ignores HOST when deriving the origin')`) passes only because it deletes
   `WEB_URL` and calls `configuration()` directly, bypassing the injection — a false guard for
   exactly the property D3 promises.

2. **`docs/dev/dev-ports.md` documents a safety net that cannot engage.** It claims setting
   `WEB_PORT` alone still yields correct CORS, but the same diff ships a non-empty
   `ALLOWED_ORIGINS` in `.env.example`, so the documented `cp .env.example .env` flow always takes
   the explicit branch. The doctor does warn here, so this is a docs-vs-behavior mismatch rather
   than an unguarded break. Same wording needs correcting in `.env.example` and the upgrade note.

Both injected to the session with file + evidence. Per canonical, FD status left unchanged and the
Linear issue stays `In Progress` — new commits will re-trigger checks and the verifier re-runs on
the new head. No verified SHA recorded (verdict was not `MERGE`).

## CI note

The first CI run on `b5be003` failed in **Set up job** with `Failed to resolve action download info.
Error: Service Unavailable` — a GitHub Actions infrastructure outage, not a code failure. Re-run
triggered; it was queued at the end of this pass.

## Needs Attention

None. PIR-260 is in a normal fix loop with the build session; no human decision is required.

## Pipeline self-correction

`agents/AGENTS.md`'s "Per status" section described the human-gated flow (`In Progress` → `In
Review`, merge on `Approved`), which contradicts this repo's `auto_merge: true` config and
`linear-pipeline.md`, where writing `In Review` **orphans** the issue (that state is excluded from
the discovery query). Corrected in place this run — see the commit on `develop`.
